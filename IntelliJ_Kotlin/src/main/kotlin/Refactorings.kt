package org.example

fun main() {
    // Rename with keyboard shortcut.
    val renameMe = "I want to be named `iHaveBeenRenamed`"
    println(renameMe)

    // Extract a method (mouse menu or shortcut, ideally both).
    val answer = 42
    println(answer)

    // Inline variable.
    val inlineMe = 1
    val three = inlineMe + 2
    println(three)
}
